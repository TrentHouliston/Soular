name: Release

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  validate-release:
    name: Validate release version
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0

      - name: Extract and validate version from release tag
        run: |
          TAG="${{ github.event.release.tag_name }}"
          VERSION="${TAG#v}"
          echo "Tag: $TAG"
          echo "Version: $VERSION"

          # PEP 440-compatible: vX.Y.Z, optional pre-release (rcN/aN/bN),
          # optional post-release (.postN), optional dev-release (.devN).
          if [[ ! "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+((rc|a|b)[0-9]+)?(\.post[0-9]+)?(\.dev[0-9]+)?$ ]]; then
            echo "::error::Release tag '$TAG' does not match expected format (vX.Y.Z[rcN|aN|bN][.postN][.devN])"
            exit 1
          fi

      - name: Check pyproject.toml version matches tag
        run: |
          TAG="${{ github.event.release.tag_name }}"
          TAG_VERSION="${TAG#v}"
          PYPROJECT_VERSION=$(uv run python -c "import tomllib; print(tomllib.load(open('pyproject.toml', 'rb'))['project']['version'])")
          echo "Tag version: $TAG_VERSION"
          echo "pyproject.toml version: $PYPROJECT_VERSION"
          if [[ "$TAG_VERSION" != "$PYPROJECT_VERSION" ]]; then
            echo "::error::Tag version ($TAG_VERSION) does not match pyproject.toml version ($PYPROJECT_VERSION)"
            exit 1
          fi

      - name: Check manifest.json version matches tag
        run: |
          TAG="${{ github.event.release.tag_name }}"
          TAG_VERSION="${TAG#v}"
          MANIFEST_VERSION=$(jq -r '.version' custom_components/soular/manifest.json)
          echo "Tag version: $TAG_VERSION"
          echo "manifest.json version: $MANIFEST_VERSION"
          if [[ "$TAG_VERSION" != "$MANIFEST_VERSION" ]]; then
            echo "::error::Tag version ($TAG_VERSION) does not match manifest.json version ($MANIFEST_VERSION)"
            exit 1
          fi

  upload-assets:
    name: Upload release assets
    needs: validate-release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Create release package
        run: |
          # Tests live beside the code they cover; they must not ship to users.
          cd custom_components/soular
          zip -r ../../soular.zip . \
            --exclude "tests/" "tests/*" "*/tests/" "*/tests/*" \
            --exclude "*/__pycache__/" "*/__pycache__/*"

      - name: Upload release assets
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          files: |
            soular.zip
