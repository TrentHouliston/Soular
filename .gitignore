# artifacts
__pycache__
.pytest*
*.egg-info
*/build/*
*/dist/*

# virtual environments
.venv

# misc
.coverage
coverage.xml
coverage.json
.ruff_cache
.pytest_cache
.mypy_cache
.grimp_cache
.import_linter_cache
.benchmarks
.DS_Store

# Home Assistant configuration
home-assistant*
known_devices.yaml
.HA_VERSION
.storage
.snapshots
.cloud
.storage/core.restore_state

# Node modules
node_modules/

# Backtest artifacts: the Open-Meteo archive cache and generated reports.
# The cache is reproducible via `uv run fetch-archive` and is far too large to track.
backtest_cache.db
backtest_out/
